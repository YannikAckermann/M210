kind: Service
apiVersion: v1
metadata:
  name: html-openshift-app
  labels:
    app: html-openshift-app
    app.kubernetes.io/part-of: html-openshift-app
spec:
  selector:
    app: html-openshift-app
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
